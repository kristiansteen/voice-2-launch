// ── Claude model IDs ──────────────────────────────────────────────────────────
// Sonnet for steps that require strict JSON schema integrity (BPMN ID references).
// Haiku for everything else — ~15× cheaper and faster, quality is sufficient.
export const MODEL_SONNET = 'claude-sonnet-4-6';
export const MODEL_HAIKU  = 'claude-haiku-4-5-20251001';
